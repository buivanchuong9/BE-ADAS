# Production-grade requirements
# Windows Server compatible - Pre-built wheels only
# Pydantic V2 - NO deprecation warnings

# Core framework
fastapi==0.115.0
uvicorn==0.30.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12  # REQUIRED for file uploads (UploadFile)

# Database - PostgreSQL (v3.0 primary) + SQL Server (legacy fallback)
sqlalchemy==2.0.25
asyncpg==0.29.0              # Native async PostgreSQL driver
psycopg2-binary==2.9.9       # Sync PostgreSQL for migrations
pyodbc==5.1.0                # SQL Server (legacy fallback)
aiosqlite==0.20.0            # For async SQLite (development)
alembic==1.13.1
greenlet==3.0.3

# Process management (user-space)
supervisor==4.2.5

# Security
python-jose[cryptography]==3.3.0
bcrypt==4.2.0

# Computer vision (pre-built wheels)
opencv-python-headless==4.10.0.84
numpy>=1.26.0,<1.27.0
pillow>=10.0.0

# Object tracking
filterpy==1.4.5
scipy>=1.10.0
scikit-learn>=1.3.0

# HTTP client
httpx==0.27.2
aiofiles==24.1.0

# Text-to-speech (Vietnamese support)
pyttsx3==2.98
gTTS==2.5.3
